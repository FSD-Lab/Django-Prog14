1. Extract the zip in the foder

2. Open Terminal and Navigate to the folder

3. Run the following command to activate the virtual environment - venv\Scripts\activate

3a. If it is not working create a virtual environment and install the required libraries using the requirements.txt provided

4. Run the Following set of commands to run the Django application

python manage.py makemigrations
python manage.py migrate
python manage.py runserver

the application will be hosted locally on http://localhost:8000/ or http://127.0.0.1:8000/ which you can open the browser and view.
